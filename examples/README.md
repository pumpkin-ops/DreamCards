# Examples

- `ai-provider.env.example`: optional multimodal provider configuration.
- `generate-card.ts`: provider-free text/image hybrid pipeline example.

All examples must be safe to publish and must use placeholders rather than working credentials.

```bash
npx tsx examples/generate-card.ts
```

The example exercises the same prompt, moderation, and fallback contracts used by the backend.
